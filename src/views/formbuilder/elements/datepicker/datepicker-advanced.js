import React from "react"

import {
    Stack,
    Typography,
    MenuItem,
    Select,
    Input,
    Box,
    TextField
} from "@mui/material"

const DatePickerAdvanced = ({editor}) => {
    return (
        <Stack spacing={2}>
            
        </Stack>
    )
}

export default DatePickerAdvanced